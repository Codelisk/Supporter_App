<common:BasePage
    xmlns:common="using:Supporter_Uno.Common.Presentation"
    x:Class="Supporter_Uno.Presentation.Chats.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Supporter_Uno.Presentation.Chats"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"

    xmlns:utu="using:Uno.Toolkit.UI"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <!--<WebView x:Name="ChatWebView" />-->
        <controls:MarkdownTextBlock Background="Red"
    Text="**Dies ist *Markdown*!****Dies ist *Markdown*!****Dies ist *Markdown*!****Dies ist *Markdown*!****Dies ist *Markdown*!**"
    Margin="6" />
        <Grid Grid.Row="1">
            <TextBox Text="Test" utu:SafeArea.Insets="Bottom"/>
        </Grid>
    </Grid>
</common:BasePage>
